Then %r{^I should recieve a valid multistatus response$} do
  pending # express the regexp above with the code you wish you had
end

Then %r{^the multistatus response should contain, for each resource:$} do |table|
  # table is a Cucumber::Ast::Table
  pending # express the regexp above with the code you wish you had
end

Then %r{^the response should contain entries with the proper depth$} do
  pending # express the regexp above with the code you wish you had
end
